# /card/:id used to be proxied to the backend here so link previews
# (Slack, iMessage, X) get OG tags. That trick — a 200! rewrite to an
# external URL — is Netlify-only; Cloudflare Pages (what this site actually
# runs on) silently ignores it and falls through to the SPA below, which is
# why every card link showed the same generic preview. The real proxy now
# lives in functions/card/[id].js as a Cloudflare Pages Function.
/* /index.html 200
